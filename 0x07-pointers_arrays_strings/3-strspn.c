#include "main.h"
#include <string.h>

/**
 * _strspn - lenght of a prefix substring
 * @s: input
 * @accept: input
 * Return: 0
 */

unsigned int _strspn(char *s, char *accept)
{
	strspn(s, accept);
	return (0);
}
